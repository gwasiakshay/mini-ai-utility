REWRITE_PROMPT = (
    "Rewrite the following text to be clearer, more formal, and professional. "
    "Return ONLY valid JSON in the following format:\n"
    '{ "rewritten_text": "string" }'
)

EXTRACT_PROMPT = (
    "Extract the key requirements or important items from the text below. "
    "Return ONLY valid JSON in this format:\n"
    '{ "items": ["string"] }'
)
